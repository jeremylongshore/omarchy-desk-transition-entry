{
  "name": "omarchy-desk-transition-entry",
  "version": "0.1.0",
  "private": true,
  "description": "Hyprland display scene model test harness",
  "scripts": {
    "test": "c8 --all --include=Model.js --check-coverage --lines 95 --functions 95 --branches 90 node --test tests/*.test.js"
  },
  "devDependencies": { "c8": "^10.1.3" }
}
